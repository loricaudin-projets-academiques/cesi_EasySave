<Application x:Class="EasySave.GUI.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:EasySave.VM.ViewModels;assembly=EasySave.VM"
             xmlns:views="clr-namespace:EasySave.GUI.Views.Pages"
             xmlns:conv="clr-namespace:EasySave.GUI.Converters">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>

            <conv:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

            <!-- ViewModel -> View mapping (strict MVVM) -->
            <DataTemplate DataType="{x:Type vm:BackupListViewModel}">
                <views:BackupListView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:BackupEditorViewModel}">
                <views:BackupEditorView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:SettingsViewModel}">
                <views:SettingsView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type vm:AboutViewModel}">
                <views:AboutView/>
            </DataTemplate>
        </ResourceDictionary>
    </Application.Resources>
</Application>
