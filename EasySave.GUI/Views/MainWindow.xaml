<Window x:Class="EasySave.GUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:EasySave.VM.ViewModels;assembly=EasySave.VM"
    mc:Ignorable="d"
    Title="EasySave" Height="600" Width="950"
    Background="{StaticResource MainBackgroundBrush}"
    Name="MainWindowRoot">

    <Grid Margin="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Background="White" CornerRadius="8" Margin="20,20,20,10" Padding="20">
            <DockPanel>
                <TextBlock Text="EasySave" FontSize="28" FontWeight="Bold" Foreground="#2C3E50"/>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                    <Button Content="⚙️" Style="{StaticResource SecondaryButtonStyle}" Width="40" Height="40" Margin="0,0,0,0" Command="{Binding OpenSettingsCommand}"/>
                    <ComboBox ItemsSource="{Binding AvailableLanguages}"
                              SelectedItem="{Binding SelectedLanguage}"
                              Width="60"
                              Margin="10,0,0,0"
                              Style="{StaticResource LanguageComboBoxStyle}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Image Source="{Binding FlagPath}" Width="32" Height="32" Stretch="Uniform" Margin="0,0,5,0"/>
                                </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Backup List -->
        <ScrollViewer Grid.Row="1" Margin="20,0,20,0">
            <StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,10">
                    <Button Content="+ Add Backup" Style="{StaticResource PrimaryButtonStyle}" Command="{Binding AddBackupCommand}" Margin="0,0,10,0"/>
                    <Button Content="▶ Run All" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding RunAllCommand}"/>
                </StackPanel>
                <ItemsControl ItemsSource="{Binding Backups}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource CardStyle}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock FontWeight="Bold" FontSize="16" Foreground="#2C3E50">
                                            <Run Text="{Binding Name, Mode=OneWay}"/>
                                            <Run Text="  ("/><Run Text="{Binding Type, Mode=OneWay}"/><Run Text=")"/>
                                        </TextBlock>
                                        <TextBlock FontSize="12" Foreground="#7F8C8D" Margin="0,2,0,0">
                                            <Run Text="Source: "/><Run Text="{Binding SourcePath, Mode=OneWay}"/>
                                        </TextBlock>
                                        <TextBlock FontSize="12" Foreground="#7F8C8D">
                                            <Run Text="Destination: "/><Run Text="{Binding DestinationPath, Mode=OneWay}"/>
                                        </TextBlock>
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center" Margin="10,0,0,0">
                                        <Button Content="▶" Style="{StaticResource PrimaryButtonStyle}" Width="40" Height="40" Command="{Binding DataContext.RunBackupCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" Margin="0,0,0,5"/>
                                        <Button Content="✏️" Style="{StaticResource SecondaryButtonStyle}" Width="40" Height="40" Command="{Binding DataContext.EditBackupCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}" Margin="0,0,0,5"/>
                                        <Button Content="🗑️" Style="{StaticResource SecondaryButtonStyle}" Width="40" Height="40" Command="{Binding DataContext.DeleteBackupCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>

        <!-- Status Bar -->
        <StatusBar Grid.Row="2" Margin="20,10,20,20" Background="White">
            <StatusBarItem Content="{Binding StatusMessage}"/>
        </StatusBar>
    </Grid>
</Window>