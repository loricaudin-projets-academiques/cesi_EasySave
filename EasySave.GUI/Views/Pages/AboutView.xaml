<UserControl x:Class="EasySave.GUI.Views.Pages.AboutView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="750">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Page Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                <Border Width="36" Height="36" CornerRadius="8" Background="{StaticResource AccentLightBrush}" Margin="0,0,12,0">
                    <TextBlock Text="&#xE946;" FontFamily="Segoe MDL2 Assets" FontSize="16" 
                               Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="{Binding Title}" FontSize="24" FontWeight="Bold" 
                           Foreground="{StaticResource TextPrimaryBrush}" FontFamily="{StaticResource AppFont}" VerticalAlignment="Center"/>
            </StackPanel>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <!-- App info card -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <Border Width="48" Height="48" CornerRadius="12" Background="{StaticResource AccentBrush}" Margin="0,0,14,0">
                            <TextBlock Text="&#xE895;" FontFamily="Segoe MDL2 Assets" FontSize="22" 
                                       Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="EasySave" FontSize="20" FontWeight="Bold" 
                                       Foreground="{StaticResource TextPrimaryBrush}" FontFamily="{StaticResource AppFont}"/>
                            <TextBlock Text="ProSoft Suite - v3.0.0" FontSize="12" 
                                       Foreground="{StaticResource TextMutedBrush}" FontFamily="{StaticResource AppFont}"/>
                        </StackPanel>
                    </StackPanel>
                    <Border Height="1" Background="{StaticResource DividerBrush}" Margin="0,0,0,16"/>
                    <TextBlock Text=".NET 8.0 | WPF | C#" FontSize="12" 
                               Foreground="{StaticResource TextSecondaryBrush}" FontFamily="{StaticResource AppFont}"/>
                </StackPanel>
            </Border>

            <!-- Paths card -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <TextBlock Text="&#xED25;" FontFamily="Segoe MDL2 Assets" FontSize="14" 
                                   Foreground="{StaticResource AccentBrush}" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="Paths" FontSize="15" FontWeight="SemiBold" 
                                   Foreground="{StaticResource TextPrimaryBrush}" FontFamily="{StaticResource AppFont}"/>
                    </StackPanel>

                    <TextBlock Text="{Binding ConfigPathLabel}" Style="{StaticResource SectionLabelStyle}"/>
                    <Border Background="{StaticResource InputBgBrush}" CornerRadius="8" Padding="12,10" Margin="0,0,0,14"
                            BorderBrush="{StaticResource InputBorderBrush}" BorderThickness="1">
                        <TextBlock Text="{Binding ConfigPath}" Foreground="{StaticResource TextPrimaryBrush}" 
                                   TextWrapping="Wrap" FontSize="12" FontFamily="{StaticResource AppFont}"/>
                    </Border>

                    <TextBlock Text="{Binding LogsPathLabel}" Style="{StaticResource SectionLabelStyle}"/>
                    <Border Background="{StaticResource InputBgBrush}" CornerRadius="8" Padding="12,10"
                            BorderBrush="{StaticResource InputBorderBrush}" BorderThickness="1">
                        <TextBlock Text="{Binding LogsPath}" Foreground="{StaticResource TextPrimaryBrush}" 
                                   TextWrapping="Wrap" FontSize="12" FontFamily="{StaticResource AppFont}"/>
                    </Border>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>

