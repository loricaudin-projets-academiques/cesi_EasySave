<UserControl x:Class="EasySave.GUI.Views.Pages.BackupEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">

    <Border Background="White" CornerRadius="8" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,16">
                <TextBlock Text="{Binding Title}" FontSize="22" FontWeight="Bold" Foreground="#2C3E50"/>
                <TextBlock Text="{Binding StatusMessage}" FontSize="12" Foreground="#7F8C8D" Margin="0,6,0,0"/>
            </StackPanel>

            <ScrollViewer Grid.Row="1">
                <StackPanel Margin="0,0,10,0">
                    <TextBlock Text="{Binding NameLabel}" FontWeight="SemiBold" Foreground="#34495E" Margin="0,0,0,6"/>
                    <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" Height="36" Margin="0,0,0,14"/>

                    <TextBlock Text="{Binding SourceLabel}" FontWeight="SemiBold" Foreground="#34495E" Margin="0,0,0,6"/>
                    <Grid Margin="0,0,0,14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{Binding SourcePath, UpdateSourceTrigger=PropertyChanged}" Height="36" Margin="0,0,10,0"/>
                        <Button Grid.Column="1" Content="{Binding BrowseButtonText}" Style="{StaticResource SecondaryButtonStyle}" Width="140" Command="{Binding BrowseSourceCommand}"/>
                    </Grid>

                    <TextBlock Text="{Binding DestinationLabel}" FontWeight="SemiBold" Foreground="#34495E" Margin="0,0,0,6"/>
                    <Grid Margin="0,0,0,14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0" Text="{Binding DestinationPath, UpdateSourceTrigger=PropertyChanged}" Height="36" Margin="0,0,10,0"/>
                        <Button Grid.Column="1" Content="{Binding BrowseButtonText}" Style="{StaticResource SecondaryButtonStyle}" Width="140" Command="{Binding BrowseDestinationCommand}"/>
                    </Grid>

                    <TextBlock Text="{Binding TypeLabel}" FontWeight="SemiBold" Foreground="#34495E" Margin="0,0,0,6"/>
                    <ComboBox ItemsSource="{Binding AvailableBackupTypes}"
                              SelectedItem="{Binding SelectedBackupType}"
                              DisplayMemberPath="DisplayName"
                              Height="36"/>
                </StackPanel>
            </ScrollViewer>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                <Button Content="{Binding CancelButtonText}" Style="{StaticResource SecondaryButtonStyle}" Width="140" Margin="0,0,10,0" Command="{Binding CancelCommand}"/>
                <Button Content="{Binding SaveButtonText}" Style="{StaticResource PrimaryButtonStyle}" Width="160" Command="{Binding SaveCommand}"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>

