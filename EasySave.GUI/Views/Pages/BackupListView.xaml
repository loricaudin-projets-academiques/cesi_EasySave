<UserControl x:Class="EasySave.GUI.Views.Pages.BackupListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Action bar -->
        <Border Grid.Row="0" Background="White" CornerRadius="8" Padding="16" Margin="0,0,0,10">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                    <Button Content="{Binding RefreshButtonText}" Style="{StaticResource SecondaryButtonStyle}" Margin="0,0,10,0" Command="{Binding RefreshListCommand}" IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBooleanConverter}}"/>
                    <Button Content="{Binding AddButtonText}" Style="{StaticResource PrimaryButtonStyle}" Margin="0,0,10,0" Command="{Binding AddBackupCommand}" IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBooleanConverter}}"/>
                    <Button Content="{Binding RunAllButtonText}" Style="{StaticResource SecondaryButtonStyle}" Command="{Binding RunAllCommand}" IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBooleanConverter}}"/>
                </StackPanel>

                <StackPanel>
                    <TextBlock Text="{Binding PageTitle}" FontSize="18" FontWeight="Bold" Foreground="#2C3E50"/>
                    <TextBlock Text="{Binding StatusMessage}" FontSize="12" Foreground="#7F8C8D" Margin="0,4,0,0"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <!-- Delete confirmation inline -->
        <Border Grid.Row="1" Background="#FEF5E7" BorderBrush="#F39C12" BorderThickness="1" CornerRadius="8" Padding="14" Margin="0,0,0,10"
                Visibility="{Binding IsDeleteConfirmVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
            <DockPanel>
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Right">
                    <Button Content="{Binding CancelText}" Style="{StaticResource SecondaryButtonStyle}" Width="120" Margin="0,0,10,0" Command="{Binding CancelDeleteCommand}"/>
                    <Button Content="{Binding ConfirmDeleteText}" Style="{StaticResource PrimaryButtonStyle}" Width="150" Command="{Binding ConfirmDeleteCommand}"/>
                </StackPanel>
                <TextBlock Text="{Binding StatusMessage}" Foreground="#2C3E50" VerticalAlignment="Center"/>
            </DockPanel>
        </Border>

        <!-- List -->
        <ScrollViewer Grid.Row="2">
            <StackPanel>
                <ProgressBar Height="10" Margin="0,0,0,10" Minimum="0" Maximum="100" Value="{Binding CurrentProgress}" />

                <ItemsControl ItemsSource="{Binding Backups}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource CardStyle}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel>
                                        <TextBlock FontWeight="Bold" FontSize="16" Foreground="#2C3E50">
                                            <Run Text="{Binding Name, Mode=OneWay}"/>
                                            <Run Text="  ("/>
                                            <Run Text="{Binding Type, Mode=OneWay}"/>
                                            <Run Text=")"/>
                                        </TextBlock>
                                        <TextBlock FontSize="12" Foreground="#7F8C8D" Margin="0,2,0,0">
                                            <Run Text="Source: "/>
                                            <Run Text="{Binding SourcePath, Mode=OneWay}"/>
                                        </TextBlock>
                                        <TextBlock FontSize="12" Foreground="#7F8C8D">
                                            <Run Text="Destination: "/>
                                            <Run Text="{Binding DestinationPath, Mode=OneWay}"/>
                                        </TextBlock>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center" Margin="10,0,0,0">
                                        <Button Content="▶" Style="{StaticResource PrimaryButtonStyle}" Width="40" Height="40"
                                                Command="{Binding DataContext.RunBackupCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}" Margin="0,0,0,5"/>
                                        <Button Content="✏️" Style="{StaticResource SecondaryButtonStyle}" Width="40" Height="40"
                                                Command="{Binding DataContext.EditBackupCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}" Margin="0,0,0,5"/>
                                        <Button Content="🗑️" Style="{StaticResource SecondaryButtonStyle}" Width="40" Height="40"
                                                Command="{Binding DataContext.RequestDeleteBackupCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>

